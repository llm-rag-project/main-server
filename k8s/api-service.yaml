apiVersion: v1
kind: Service
metadata:
  name: news-api
  namespace: news-monitor
spec:
  selector:
    app: news-api
  ports:
    - name: http
      port: 8001
      targetPort: http
  type: ClusterIP
